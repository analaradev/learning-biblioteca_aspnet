@using Taller_ASP.NET_Core.Models
@model List<Book>
@{
    ViewData["Title"] = "Mi Biblioteca";
}

<div class="container-fluid mt-4">
    <!-- Encabezado -->
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h2><i class="bi bi-book"></i> Mi Biblioteca</h2>
        <a asp-action="Create" class="btn btn-primary">
            <i class="bi bi-plus-lg"></i> Nuevo Libro
        </a>
    </div>

    <!-- Barra de búsqueda y filtros -->
    <form method="get" asp-action="Index" id="filterForm" class="row g-3 mb-3">
        <div class="col-md-7">
            <div class="input-group">
                <span class="input-group-text">
                    <i class="bi bi-search"></i>
                </span>
                <input type="text" 
                       name="searchTerm" 
                       class="form-control" 
                       placeholder="Buscar libros por título, autor, ISBN o género..."
                       value="@ViewBag.SearchTerm"
                       oninput="this.form.submit()">
            </div>
        </div>
        <div class="col-md-3">
            <select name="filter" class="form-select" onchange="this.form.submit()">
                <option value="all" selected="@(ViewBag.Filter == "all" ? "selected" : null)">Todos los libros</option>
                <option value="pending" selected="@(ViewBag.Filter == "pending" ? "selected" : null)">Pendientes de leer</option>
                <option value="completed" selected="@(ViewBag.Filter == "completed" ? "selected" : null)">Leídos</option>
            </select>
        </div>
        <div class="col-md-2">
            @if (!string.IsNullOrEmpty(ViewBag.SearchTerm) || ViewBag.Filter != "all")
            {
                    <a asp-action="Index" class="btn btn-outline-secondary w-100">
                        <i class="bi bi-x-circle"></i> Limpiar
                    </a>
            }
        </div>
    </form>

    <!-- Contador de libros -->
    <div class="mb-3">
        <small class="text-muted">
            Mostrando @Model.Count libro@(Model.Count != 1 ? "s" : "")
        </small>
    </div>

    <div class="row">
        <!-- Panel izquierdo: Lista de libros -->
        <div class="col-md-4">
            <div id="book-list" class="book-list">
                @if (Model.Count == 0)
                {
                        <div class="alert alert-info text-center">
                            <i class="bi bi-info-circle" style="font-size: 2rem;"></i>
                            <p class="mt-2 mb-0">
                            @if (!string.IsNullOrEmpty(ViewBag.SearchTerm))
                            {
                                <text>No se encontraron libros con el término "@ViewBag.SearchTerm"</text>
                            }
                            else if (ViewBag.Filter == "pending")
                            {
                                <text>No tienes libros pendientes de leer</text>
                            }
                            else if (ViewBag.Filter == "completed")
                            {
                                <text>No tienes libros leídos</text>
                            }
                            else
                            {
                                <text>No tienes libros en tu biblioteca. ¡Agrega tu primer libro!</text>
                            }
                            </p>
                        </div>
                }
                else
                {
                    @foreach (var book in Model)
                    {
                        @await Html.PartialAsync("_BookCard", book)
                    }
                }
            </div>
        </div>

        <!-- Panel derecho: Detalle del libro -->
        <div class="col-md-8">
            <div class="card shadow-sm">
                <div class="card-header bg-secondary text-white">
                    <h5 class="mb-0"><i class="bi bi-info-circle"></i> Detalles del Libro</h5>
                </div>
                <div class="card-body">
                    <div id="book-detail-container" class="text-center text-muted">
                        <i class="bi bi-arrow-left-circle" style="font-size: 3rem;"></i>
                        <p class="mt-3">Selecciona un libro de la lista para ver los detalles</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
        <script src="https://cdn.jsdelivr.net/npm/sortablejs@latest/Sortable.min.js"></script>
        <script src="~/js/books.js"></script>
}
